# Legacy Webflow export (backup)

Unmodified copies of the original Webflow CSS and JS from the export, kept for reference and rollback.

Active site styles load from:

- `css/vault-base.css` (was `webflow.css`)
- `css/vault-theme.css` (was `vaultgym-…webflow.css`)

The runtime script `js/webflow.js` was removed from the repo; this folder retains a copy if you ever need to audit behavior.
