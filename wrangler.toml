# Cloudflare Pages — valid config so the dashboard does not skip this file.
# Astro 6 needs Node >= 22.12. Set NODE_VERSION=22 (or 22.12.0) in:
# Pages → Settings → Environment variables (Production + Preview).

name = "thevaultgym"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
