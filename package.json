{
  "name": "build101",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "generate": "node scripts/generate-page-data.mjs",
    "sync-public": "node scripts/sync-public.mjs",
    "predev": "npm run generate && npm run sync-public",
    "seed-cms": "node scripts/seed-cms-from-generated.mjs",
    "seed-sanity": "node --env-file=.env scripts/seed-sanity-from-json.mjs",
    "prebuild": "npm run generate && npm run sync-public",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "studio": "npm run dev --prefix studio"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Samu77-B/thevaultgym.git"
  },
  "license": "ISC",
  "dependencies": {
    "@sanity/client": "^6.28.0",
    "@sanity/image-url": "^1.1.0",
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "astro": "^6.1.4"
  }
}
